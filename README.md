# temporal-clone

`temporal-clone` makes TC39 Temporal values survive structured-clone boundaries (`postMessage`, workers, `BroadcastChannel`, IndexedDB, `history.state`) without data loss, using a small versioned wire format that works with native Temporal and the common polyfills.

**Under construction.** Nothing is published yet.
